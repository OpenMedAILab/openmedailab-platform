# OpenMedAILab 网站测试报告

报告日期：2026-06-19  
测试对象：OpenMedAILab 医学 AI 开放课题协作平台  
测试版本：1.0.0 正式版发布候选  
测试类型：系统测试、回归测试、接口测试、权限测试、业务流程测试、前端构建、开发版浏览器 E2E、正式环境只读冒烟测试  
测试人员：高级软件测试工程师视角执行

## 1. 测试结论

本轮测试覆盖公开课题库、登录注册入口、个人中心、课题协作、认领与资助审批、管理员工作台、主题与课题管理、PDF 上传、讨论区、积分、审计、备份、FAQ、Swagger/OpenAPI、前端构建、开发版浏览器 E2E 和正式环境只读冒烟。

综合结论：当前版本核心业务链路通过测试，未发现阻塞上线的问题。建议进入上线候选或灰度验证阶段。

| 项目 | 结果 |
| --- | --- |
| 阻塞缺陷 | 0 |
| 严重缺陷 | 0 |
| 一般功能缺陷 | 0 |
| 测试基建风险 | 4 |
| 业务主流程 | 通过 |
| 管理员审批模块 | 通过 |
| 前端构建 | 通过 |
| 开发版浏览器 E2E | 通过 |
| 正式环境只读冒烟 | 通过 |

## 2. 测试范围

### 2.1 已覆盖模块

| 模块 | 覆盖内容 | 结果 |
| --- | --- | --- |
| 系统基础 | 健康检查、CSRF、版本信息、OpenAPI、Swagger | 通过 |
| 公开课题库 | 课题列表、主题筛选、推荐排序、公开详情、进度页、状态卡 | 通过 |
| 用户体系 | 访客、普通用户、认领用户、资助用户、平台管理员 | 通过 |
| RBAC 权限 | 访客禁止写操作、普通用户禁止管理接口、管理员可进入管理端 | 通过 |
| 课题互动 | 点赞、收藏、参与、认领负责人、认领论文第一单位、资助、撤回 | 通过 |
| 审批管理 | 待审批列表、通过、拒绝、拒绝必填意见、禁止审批普通参与 | 通过 |
| 个人中心 | 个人资料、我的关系、积分流水、积分转赠 | 通过 |
| 主题管理 | 新增、停用、启用、删除主题 | 通过 |
| 课题管理 | 新增草稿、更新公开、主 PDF 上传、物理删除 | 通过 |
| 数据集说明 | 主题级数据集说明记录、数据集说明 PDF 上传 | 通过 |
| 讨论区 | 公开读取、登录用户创建/编辑、管理员隐藏、作者删除 | 通过 |
| 审计日志 | 写操作审计、失败权限审计、审计列表读取 | 通过 |
| 内容备份 | 管理员导出、普通用户禁止导出 | 通过 |
| 前端 UI | 首页、FAQ、登录页、管理页、申请审批页、移动端响应式、弹窗与悬浮卡 | 通过 |
| 浏览器 E2E | Chrome 真实浏览器、桌面/平板/手机 5 视口、关键协作与审批流 | 通过 |
| 正式站冒烟 | 首页、健康检查、meta、公开课题、OpenAPI | 通过 |

### 2.2 本轮未覆盖或非重点

| 项目 | 说明 |
| --- | --- |
| 压力测试 | 未做并发压测或容量测试。 |
| 安全渗透测试 | 未做系统级渗透、弱口令扫描、越权自动化 fuzz。 |
| 多浏览器兼容 | 已完成系统 Chrome 完整 E2E，未覆盖 Edge/Firefox/Safari 矩阵。 |
| 真实文件大体积上传 | 已覆盖 PDF 上传正向路径，未做 20MB 边界大文件真实上传压力验证。 |

## 3. 测试环境

### 3.1 本地环境

| 项目 | 值 |
| --- | --- |
| 后端服务 | `http://127.0.0.1:8000` |
| 前端服务 | `http://127.0.0.1:5173` |
| Python 环境 | conda `base` 环境 |
| 前端构建工具 | Vite |
| 数据库 | 本地 SQLite 测试库 / 本地 E2E 数据 |
| 版本 | 1.0.0 |

说明：项目要求任何 Python 命令必须使用 conda 环境，本轮测试均遵守该要求。

### 3.2 正式环境只读冒烟

| 项目 | 地址 |
| --- | --- |
| 正式站 | `http://124.221.149.91/` |
| 健康检查 | `http://124.221.149.91/api/health/` |
| 元信息 | `http://124.221.149.91/api/meta/` |
| 公开课题 | `http://124.221.149.91/api/projects/?page_size=5&sort=recommended` |
| OpenAPI | `http://124.221.149.91/api/openapi.json` |

正式环境测试仅做只读访问，未登录、未写入、未修改正式数据。

### 3.3 开发版浏览器 E2E

| 项目 | 地址 / 说明 |
| --- | --- |
| 开发版站点 | `http://124.221.149.91:3406/` |
| 开发版后端 | `127.0.0.1:3407`，由 nginx 代理 |
| 浏览器 | 本机系统 Google Chrome，由 Playwright `channel: "chrome"` 驱动 |
| 视口矩阵 | desktop-1440、tablet-768、mobile-360、mobile-390、mobile-320 |
| 数据策略 | 只写开发版 E2E fixture，不写正式库 |
| 部署策略 | 部署前备份开发版目录，保留开发版数据库和媒体文件 |

## 4. 测试数据

本地业务流测试和开发版浏览器 E2E 均使用 E2E fixture 生成测试数据，包含：

| 类型 | 数量 / 说明 |
| --- | --- |
| 测试用户 | 普通参与用户、认领用户、资助用户、平台管理员 |
| 测试课题 | 6 个 E2E 课题，覆盖开放招募、组队中、进行中、认领占用、待审批等状态 |
| 管理员账号 | `platform_admin`，UID 为 `ADM00000001` |
| 测试密码 | 本地 E2E fixture 使用 `StrongPass12345` |

测试执行前已重置 E2E 数据，保证业务流测试可重复。开发版重置只作用于开发版测试数据，不影响正式环境数据。

## 5. 自动化与命令执行结果

### 5.1 后端与数据库检查

| 命令 | 结果 |
| --- | --- |
| `conda run -n base python manage.py check` | 通过 |
| `conda run -n base python manage.py makemigrations --check --dry-run` | 通过，无迁移遗漏 |
| `conda run -n base python manage.py test accounts api projects interactions credits` | 通过，110/110 |
| `conda run -n base python scripts/check_release_version.py` | 通过，版本 `1.0.0` |

备注：第一次完整后端测试中，`InteractionConcurrencyTests.test_participation_credit_precheck_uses_locked_profile_balance` 出现一次并发场景偶发失败。该用例单独复跑通过，完整测试套件复跑也通过，判断为 SQLite/本地并发测试偶发风险，未形成稳定产品缺陷。

### 5.2 前端检查

| 命令 | 结果 |
| --- | --- |
| `node --check frontend/src/main.js` | 通过 |
| `node --test frontend/src/*.test.js` | 通过，70/70 |
| `npm --prefix frontend run build` | 通过 |
| `git diff --check` | 通过 |

构建输出：

| 文件 | 体积 |
| --- | --- |
| `dist/index.html` | 0.88 kB |
| `dist/assets/index-CEGT3oYD.css` | 66.42 kB |
| `dist/assets/index-CoHd9_hH.js` | 484.39 kB |

### 5.3 本地 HTTP 业务流测试

本轮额外编写临时测试脚本 `/private/tmp/openmedailab_api_qa.py`，通过真实 HTTP 请求访问本地后端，覆盖多角色业务闭环。脚本未写入仓库。

| 指标 | 结果 |
| --- | --- |
| 测试项总数 | 78 |
| 通过 | 78 |
| 失败 | 0 |

覆盖的关键断言包括：

| 业务点 | 验证结果 |
| --- | --- |
| 访客读取公开数据 | 通过 |
| 访客禁止收藏、参与 | 通过 |
| 普通用户登录、个人中心、积分流水 | 通过 |
| 普通用户收藏、点赞后 `viewer_state` 可恢复 | 通过 |
| 普通参与自动 `approved` | 通过 |
| 参与后状态卡展示 UID 分组 | 通过 |
| 负责人认领进入 `pending` | 通过 |
| 论文第一单位认领缺少单位名返回 422 | 通过 |
| 论文第一单位认领记录单位名称 | 通过 |
| 已占用负责人席位重复认领返回 409 | 通过 |
| 非招募阶段禁止参与 | 通过 |
| 资助提交进入 `pending` | 通过 |
| 管理员待审批列表包含负责人、第一单位、资助 | 通过 |
| 拒绝审批不填意见返回 422 | 通过 |
| 管理员通过负责人认领 | 通过 |
| 管理员拒绝第一单位并保留审核意见 | 通过 |
| 管理员通过资助 | 通过 |
| 管理员禁止审批普通参与 | 通过 |
| 审批后状态卡展示已通过认领和资助 | 通过 |
| 用户撤回已通过资助、负责人认领、参与 | 通过 |
| 讨论区创建、编辑、隐藏、删除 | 通过 |
| 主题新增、停用、启用、删除 | 通过 |
| 主题数据集说明记录和 PDF 上传 | 通过 |
| 管理员课题新增、更新、主 PDF 上传、物理删除 | 通过 |
| 内容备份导出和普通用户禁止导出 | 通过 |
| 审计日志包含本轮操作 | 通过 |
| 非进行中或无资格提交任务结果被拦截 | 通过 |
| 积分转赠超过 50 失败，1 分转赠成功 | 通过 |

### 5.4 页面级烟测

使用内置浏览器对本地前端进行只读页面检查。

| 页面 | 检查点 | 结果 |
| --- | --- | --- |
| 首页 | 页面非空、主题区存在、课题卡存在、旧首页大段文案已移除 | 通过 |
| FAQ | 常见问答页面正常渲染 | 通过 |
| 登录页 | 用户名、密码、登录按钮存在 | 通过 |
| 未登录管理页 | 展示无权限提示，不空白 | 通过 |
| 管理员工作台 | 登录后可见总览、申请审批、任务管理、课题管理、主题管理、用户管理、系统入口、备份恢复、审计日志 | 通过 |
| 申请审批页 | 可见待处理认领记录和通过/拒绝入口 | 通过 |
| 控制台错误 | 未发现 JS error | 通过 |

页面检查中看到 DOM 文本提取包含 `thumb_up`、`star`、`groups` 等 Material Symbols ligature 文本。进一步检查样式后确认图标元素使用 `Material Symbols Rounded` 字体，宽高为 16-20px，且 `aria-hidden=true`，未判断为真实用户可见缺陷。

### 5.5 开发版 Playwright 浏览器 E2E

本轮补跑完整 Playwright 页面级 E2E。为避免正式数据被写入，先将当前代码部署到开发版 `http://124.221.149.91:3406/`，并在开发版执行测试数据重置。

| 指标 | 结果 |
| --- | --- |
| 浏览器 | 系统 Google Chrome |
| 视口 | 5 个 |
| 用例总数 | 55 |
| 业务断言通过 | 55/55 |
| 第一轮完整执行 | 52/55 通过，3 条失败均为 SSH/SCP 重置测试数据前置步骤偶发失败 |
| 失败用例重跑 | 3/3 通过 |
| 最终结论 | 浏览器 E2E 通过，未发现稳定业务缺陷 |

覆盖的关键页面和业务点包括：

| 覆盖点 | 结果 |
| --- | --- |
| 更新日志弹窗焦点陷阱、ESC 关闭和焦点恢复 | 通过 |
| 未登录操作 toast 可访问性 | 通过 |
| 确认弹窗层级高于业务弹窗 | 通过 |
| 首页无横向溢出，首屏展示课题标题和状态条 | 通过 |
| 课题关系角标、主题标签、状态条、悬停颜色 | 通过 |
| 参与后角标即时出现且卡片布局不抖动 | 通过 |
| 课题详情协作 CTA：点赞、关注、参与、资助 | 通过 |
| 认领不可用原因提示、联系人悬浮卡、资助弹层视口约束 | 通过 |
| 待审批标签不溢出 | 通过 |
| 管理员审批资助和二级资助选项 | 通过 |
| 个人中心驳回申请预填后重新提交 | 通过 |
| 320/360/390 手机宽度、768 平板、1440 桌面响应式 | 通过 |

说明：最初 `page.goto(..., waitUntil: "load")` 在开发版会被 Google Fonts/Material Symbols 外部字体加载拖慢，页面实际已渲染。临时 E2E 配置改为 `domcontentloaded` 并用显式元素等待验证页面真实可用性。该调整只发生在临时测试副本，未修改仓库业务代码。

### 5.6 正式站只读冒烟

| 接口 / 页面 | 状态 | 摘要 |
| --- | --- | --- |
| `/api/health/` | 200 | `{"status": "ok"}` |
| `/api/meta/` | 200 | 发布前冒烟版本 `0.15.3`，主题数 6；1.0.0 部署后复验 |
| `/api/projects/?page_size=5&sort=recommended` | 200 | 公开课题总数 12，返回前 5 个课题 |
| `/api/openapi.json` | 200 | 发布前冒烟版本 `0.15.3`，路径数 71；1.0.0 部署后复验 |
| `/` | 200 | 首页 HTML 正常返回 |

## 6. 业务流程测试详情

### 6.1 访客流程

| 用例 | 预期 | 结果 |
| --- | --- | --- |
| 访问首页课题列表 | 只展示公开且非归档课题 | 通过 |
| 访问课题详情 | 返回公开课题详情 | 通过 |
| 访问课题进度页 | 返回公开进度信息 | 通过 |
| 访问状态卡 | 匿名访问正常，不暴露敏感信息 | 通过 |
| 尝试收藏课题 | 返回 401/403 | 通过 |
| 尝试参与课题 | 返回 401/403 | 通过 |

### 6.2 普通用户流程

| 用例 | 预期 | 结果 |
| --- | --- | --- |
| 登录 | 返回用户信息和 RBAC 能力 | 通过 |
| 读取个人中心 | 返回用户、关系、任务、积分等聚合数据 | 通过 |
| 收藏课题 | 成功写入关系 | 通过 |
| 点赞课题 | 成功写入评分 | 通过 |
| 列表恢复按钮状态 | `viewer_state` 正确返回收藏/点赞状态 | 通过 |
| 参与课题 | 自动通过，状态为 `approved` | 通过 |
| 撤回参与 | 状态变为 `withdrawn` | 通过 |
| 积分转赠 | 单次超过 50 拒绝，正常额度成功 | 通过 |

### 6.3 认领与资助流程

| 用例 | 预期 | 结果 |
| --- | --- | --- |
| 认领项目负责人 | 进入待审批 | 通过 |
| 论文第一单位认领缺少单位 | 返回 422 | 通过 |
| 论文第一单位认领填写单位 | 进入待审批并保存单位名 | 通过 |
| 负责人席位已占用后重复认领 | 返回 409 | 通过 |
| 提交资助意向 | 进入待审批 | 通过 |
| 非招募阶段提交参与 | 返回 422 | 通过 |

### 6.4 管理员审批流程

| 用例 | 预期 | 结果 |
| --- | --- | --- |
| 普通用户访问审批列表 | 返回 403 | 通过 |
| 管理员访问审批列表 | 返回认领和资助待审批记录 | 通过 |
| 拒绝审批不填意见 | 返回 422，错误码为审核意见必填 | 通过 |
| 通过负责人认领 | 状态变为 `approved`，写入 reviewed_by | 通过 |
| 拒绝第一单位认领 | 状态变为 `rejected`，保留审核意见 | 通过 |
| 通过资助 | 状态变为 `approved`，保留审核意见 | 通过 |
| 尝试审批普通参与 | 返回 422，禁止管理员审批普通参与 | 通过 |
| 审批后刷新状态卡 | 展示已通过认领和资助分组 | 通过 |

### 6.5 管理端内容维护

| 用例 | 预期 | 结果 |
| --- | --- | --- |
| 新增主题 | 成功 | 通过 |
| 停用主题 | `is_active=false` | 通过 |
| 启用主题 | `is_active=true` | 通过 |
| 新增主题数据集说明 | 成功 | 通过 |
| 上传数据集说明 PDF | 成功并返回 PDF 路径 | 通过 |
| 新增课题 | 默认草稿 | 通过 |
| 更新课题公开招募 | 成功 | 通过 |
| 上传课题主 PDF | 成功并返回文档记录 | 通过 |
| 物理删除课题 | 成功 | 通过 |
| 删除主题数据集说明 | 成功 | 通过 |
| 删除主题 | 成功 | 通过 |

### 6.6 审计与备份

| 用例 | 预期 | 结果 |
| --- | --- | --- |
| 管理员导出内容备份 | 返回 ZIP | 通过 |
| 普通用户导出备份 | 返回 403 | 通过 |
| 管理员查看审计日志 | 返回审计记录 | 通过 |
| 本轮写操作审计 | 审计中可见相关操作 | 通过 |

## 7. 缺陷与风险

### 7.1 阻塞缺陷

无。

### 7.2 一般缺陷

无稳定复现的一般业务缺陷。

### 7.3 测试基建风险

| 编号 | 风险 | 级别 | 说明 | 建议 |
| --- | --- | --- | --- | --- |
| R-001 | 开发版 E2E 夹具重置依赖 SSH/SCP | 中 | 第一轮完整 E2E 有 3 条失败发生在测试前置 SSH/SCP 步骤，重试后全部通过，未发现业务缺陷。 | 将开发版 E2E 重置脚本做成稳定入口，增加重试、超时配置和日志输出。 |
| R-002 | E2E 脚本硬编码 `.venv/bin/python` | 中 | 仓库现有 Playwright setup/helper 使用 `.venv/bin/python`，与项目要求“Python 必须使用 conda 环境”冲突；本轮通过临时配置绕开。 | 将 Python 路径改为可配置，例如读取 `PYTHON_BIN`，默认使用 conda 命令。 |
| R-003 | SQLite 并发测试存在偶发性 | 低 | 后端并发积分预检查用例第一次完整套件出现一次 500，单测和完整复跑均通过。 | 后续在更接近生产的数据库环境做并发回归，或在 CI 对该用例重复运行。 |
| R-004 | 测试环境 staticfiles warning | 低 | Django 测试出现 `staticfiles/` 目录不存在 warning，不影响结果。 | 可创建目录或调整测试配置降低噪音。 |

## 8. 上线建议

1. 当前版本核心业务链路可进入上线候选。
2. 上线前建议至少保留一次正式环境只读冒烟，确认版本号、首页、课题列表、OpenAPI 正常。
3. 管理员审批模块已经通过本地完整业务链路测试，但首次真实运营时建议管理员重点关注待审批列表、拒绝意见、用户侧审核意见展示。
4. 建议将本轮临时开发版 Playwright 配置沉淀为正式脚本，支持开发版地址、远程夹具重置、系统 Chrome 和 conda Python。
5. 建议把本轮 HTTP 业务流脚本沉淀为正式 QA 脚本，并纳入发布前检查。
6. 建议后续补充性能测试，尤其是 `/api/meta/`、`/api/openapi.json`、公开课题列表和管理员列表的大数据量表现。

## 9. 准出标准核对

| 准出项 | 状态 |
| --- | --- |
| 后端系统检查通过 | 已满足 |
| 数据库迁移检查无遗漏 | 已满足 |
| 后端测试通过 | 已满足 |
| 前端语法检查通过 | 已满足 |
| 前端单元/静态测试通过 | 已满足 |
| 前端生产构建通过 | 已满足 |
| 版本一致性检查通过 | 已满足 |
| 本地多角色业务流通过 | 已满足 |
| 管理员审批模块通过 | 已满足 |
| 完整 Playwright E2E | 已满足 |
| 正式环境只读冒烟通过 | 已满足 |

准出判断：核心准出项均已满足，可进入上线候选。剩余事项属于测试基建稳定性和性能/安全专项补测，不构成本轮上线阻塞。

## 10. 附录：本轮关键证据

### 10.1 本地测试摘要

```text
Django system check: PASS
Migration check: PASS, No changes detected
Backend tests: 110/110 PASS
Frontend tests: 70/70 PASS
Frontend build: PASS
Release version check: PASS, 1.0.0
HTTP business flow tests: 78/78 PASS
Browser UI smoke: PASS, no console errors
Development Playwright E2E: 55/55 business assertions PASS
First E2E run: 52/55 PASS, 3 setup SSH/SCP failures
Last-failed rerun: 3/3 PASS
```

### 10.2 正式站只读冒烟摘要

```text
/api/health/: 200, ok=true
/api/meta/: 200, pre-release version=0.15.3, themes=6
/api/projects/?page_size=5&sort=recommended: 200, total_count=12
/api/openapi.json: 200, pre-release version=0.15.3, paths=71
/: 200
```

### 10.3 测试结束状态

```text
Local backend 127.0.0.1:8000: stopped
Local frontend 127.0.0.1:5173: stopped
Production data: read-only smoke only, no write operation
Development data: E2E fixture reset and write-flow verification performed on port 3406
Repository code: no business code modified during testing
```
